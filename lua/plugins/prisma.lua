-- Using vim-prisma for syntax highlighting. Ideally there would
-- be a cmp compatible plugin, but right now they only support
-- coc with this plugin: https://github.com/pantharshit00/coc-prisma
return {
    'prisma/vim-prisma',
    ft = 'prisma'
}

